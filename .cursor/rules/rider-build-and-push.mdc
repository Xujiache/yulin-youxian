---
description: 骑手端改动后的固定收尾：验证、签名发布、推 GitHub、给出产物路径
globs: rider-android/**
alwaysApply: false
---

# 改完骑手端必须做的三件事

顺序固定：**先验证 → 再签名发布 → 再推送 → 最后给路径**。任何一步失败就停下来说清楚，不要跳过。

本仓库的生产签名包由 **GitHub Actions** 打，私钥只存在 repo Secrets，不进生产服务器、不进仓库。自动发布默认是**可选更新**；只有明确标记强制时才阻断旧版本。

## 1. 验证

有 Android SDK / 签名材料的机器上：

```powershell
$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"
$env:ANDROID_SDK_ROOT="C:\Users\Administrator\AppData\Local\Android\Sdk"
$env:ANDROID_HOME=$env:ANDROID_SDK_ROOT
.\gradlew.bat testDebugUnitTest verifyReleaseSignature -PRIDER_BUILD_SEQUENCE=<当日序号>
```

- `verifyReleaseSignature` 自带 `assembleRelease` 并校验签名，不要只跑 `assembleRelease`。
- **versionCode 是 `YYMMDD` + 两位当日序号**。序号必须比设备上已装的大，否则装机被当作降级拒绝。
  先 `adb shell dumpsys package com.yulin.rider | Select-String versionCode` 看一眼再定。
- 签名口令读 `%USERPROFILE%\.yulin\rider-signing.properties`，高德 Key 读全局 `~/.gradle/gradle.properties`，都不要写进仓库。
- **不要为了快牺牲准度**：不跳测试、不跳签名校验、不用 `-PRIDER_ABI=arm64` 减体积，除非用户明确要小包。

这台 Linux 部署机通常 **没有 Android SDK**。不要在这里假装打了签名包；把提交推上去，用下面的标签走 Actions。

## 2. 推送 GitHub 并打发布标签

按模块分提交，不要把不相干的改动堆成一个。推之前 `git fetch`，远程有新提交就先
`git rebase origin/<branch>` 保持线性历史。禁止强推。

后端/管理台在本机部署验证成功后，打**注解标签**触发自动签名发布：

```bash
git tag -a rider-vYYYY.MM.DD.N -m "更新标题

更新日志正文"
git push origin rider-vYYYY.MM.DD.N
```

- 标签格式必须是 `rider-vYYYY.MM.DD.N`（N 为当日序号，对应 versionCode 末两位）。
- 注解内容就是骑手看到的更新日志；源码 SHA 由 Actions 写入版本历史。
- 默认 OPTIONAL。需要强制更新时在 tag message 首行加 `FORCE:` 或在 workflow 输入里标明。

## 3. 给出可核对的产物信息

本机若打出了 APK，回复末尾用独立代码块给出**绝对路径**，并说明 versionCode、大小、SHA-256。

若走 Actions：给出 tag 名、workflow 链接、以及发布后 `GET /api/public/rider/app/latest` 的 versionCode。
