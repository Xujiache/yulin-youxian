<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 定位(06 §3.2 保活第一层) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- 前台服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- 通知 / 保活 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Android 14+ 锁屏全屏派单页必需;缺失时只会降级成横幅通知,不会崩 -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- 网络 / 拍照 / 反馈 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Android 11+ 包可见性:保活向导要跳厂商设置页,不声明会 queryIntentActivities 全空 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <package android:name="com.autonavi.minimap" />
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.hihonor.systemmanager" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.coloros.oppoguardelf" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.vivo.abe" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.samsung.android.lool" />
        <package android:name="com.samsung.android.sm_cn" />
        <package android:name="com.meizu.safe" />
        <package android:name="cn.nubia.security2" />
        <package android:name="com.zte.heartyservice" />
        <package android:name="com.transsion.phonemaster" />
        <package android:name="com.lenovo.security" />
    </queries>

    <application
        android:name=".RiderApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="false">
        <!-- 明文 HTTP 只在 debug 打开(src/debug 的 network_security_config 覆盖本文件),
             release 走 https://hqhjxt.vip,不允许明文 -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode|density|fontScale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 定位前台服务(实现见 core/location),location 类型无运行时长上限 -->
        <service
            android:name="com.yulin.rider.core.location.LocationForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <receiver
            android:name=".DutyRecoveryReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- 高德 Key 暂缺:占位符默认空串,SDK 会鉴权失败但不崩溃。
             拿到 Key 后在 gradle.properties 或 -PAMAP_KEY=xxx 注入即可,无需改代码。 -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="${AMAP_KEY}" />

        <!-- 高德依赖解析出来是纯 jar(不带 AndroidManifest),SDK 自身组件不会自动合并,
             必须在这里手工声明。缺了 APSService 高德定位完全起不来。 -->
        <service
            android:name="com.amap.api.location.APSService"
            android:exported="false" />

        <!-- AmapNaviPage.showRouteActivity 的宿主。不声明时导航会自动降级为 geo: Intent,不崩但用不上 SDK 导航 -->
        <activity
            android:name="com.amap.api.navi.AmapRouteActivity"
            android:exported="false"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar" />

    </application>

</manifest>
