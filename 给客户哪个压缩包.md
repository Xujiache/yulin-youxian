# 🎯 给客户哪个压缩包？

## 答案：现有的压缩包就可以给客户

---

## 📦 推荐给客户的压缩包

### 方案 1：直接使用现有压缩包（推荐）✅

**文件名**：`print-agent/禹邻优鲜-小票打印代理.zip`

**说明**：这个压缩包已经包含了客户需要的所有文件。

---

## 📋 压缩包内容

解压后包含以下文件：

| 文件名 | 说明 | 是否必需 |
|--------|------|---------|
| `YulinPrintAgent.exe` | 主程序 | ✅ 必需 |
| `printer.sdk.dll` | 打印机驱动 | ✅ 必需 |
| `setup-agent.cmd` | 配置向导 | ✅ 必需 |
| `run-agent.cmd` | 日常启动脚本 | ✅ 必需 |
| `install-autostart.cmd` | 开机自启设置 | ✅ 推荐 |
| `uninstall-autostart.cmd` | 取消开机自启 | ⭐ 可选 |
| `安装说明.txt` | 安装指南 | ✅ 必需 |
| `使用说明.md` | 详细文档 | ⭐ 建议 |
| `README.txt` | 快速指南 | ⭐ 建议 |

---

## 📤 交付给客户的完整包

### 交付清单

```
📦 禹邻优鲜-小票打印系统
├─ 📁 管理后台访问地址：https://你的域名
├─ 📁 小程序码：（二维码图片）
└─ 📦 打印代理：禹邻优鲜-小票打印代理.zip
```

---

## 💡 给客户的使用指引

### 简单三步，开始使用

**第一步：解压文件**
```
将 禹邻优鲜-小票打印代理.zip 解压到桌面
```

**第二步：首次配置**
```
双击运行：setup-agent.cmd

按提示填写：
1. API 地址：http://你的服务器地址/api
2. 打印密钥：（在管理后台生成）
3. 选择打印机：直接回车（自动检测）
```

**第三步：设置开机自启（推荐）**
```
双击运行：install-autostart.cmd

完成！之后电脑开机自动打印，无需任何操作
```

---

## 📝 给客户的注意事项

### 必读说明

1. **解压位置**：
   - ✅ 建议：`C:\禹邻优鲜\打印代理`
   - ⚠️ 不要放在桌面或临时文件夹

2. **不要删除文件**：
   - 所有文件都是必需的
   - 特别是 `agent.config.json`（配置文件，自动生成）

3. **配置只需一次**：
   - 首次运行 `setup-agent.cmd`
   - 之后自动保存配置
   - 无需重复输入

4. **推荐开机自启**：
   - 门店使用强烈推荐
   - 设置后完全自动
   - 无人值守

---

## 🎓 培训要点

### 培训客户时重点说明

#### ✅ 强调这些
1. **配置只需一次**（打消"每次都要输入"的顾虑）
2. **建议使用开机自启**（最省心）
3. **配置会自动保存**（电脑重启不丢失）
4. **保持网络连接**（能访问后端服务器）

#### ⚠️ 避免这些误区
1. ❌ 不要每次都运行 `setup-agent.cmd`
   - ✅ 正确：只在首次或更换设备时运行
   
2. ❌ 不要删除 `agent.config.json`
   - ✅ 这是配置文件，删除需重新配置

3. ❌ 不要关闭打印代理窗口（未设置自启时）
   - ✅ 关闭窗口会停止自动打印

---

## 📞 常见客户问题预答

### Q1：需要安装什么软件吗？
**A**：不需要！解压即用，双击运行即可。

### Q2：每次都要输入密钥吗？
**A**：不需要！密钥会自动保存，只需首次输入。

### Q3：电脑重启后怎么办？
**A**：
- 方案 A（推荐）：设置开机自启，自动运行
- 方案 B：手动运行 `run-agent.cmd`

### Q4：支持什么打印机？
**A**：支持所有芯烨（Xprinter）品牌的 USB 热敏打印机。

### Q5：可以同时连多台打印机吗？
**A**：一个打印代理只能连一台。需要多台的话，复制文件夹分别配置。

---

## 🚀 快速部署清单

### 给客户的完整交付包

```
1. 压缩包：禹邻优鲜-小票打印代理.zip
2. 文档：安装说明.txt（压缩包内已包含）
3. 信息：
   - API 地址
   - 打印密钥（管理后台生成）
   - 管理后台地址
```

### 部署流程

```
1. 发送压缩包给客户
2. 远程协助或电话指导：
   ① 解压到 C:\禹邻优鲜\打印代理
   ② 双击 setup-agent.cmd
   ③ 填写 API 地址和密钥
   ④ 双击 install-autostart.cmd
3. 测试打印：管理后台 → 测试打印
4. 完成！
```

---

## 📊 对比说明

### 现有压缩包 vs 需要重新打包

| 项目 | 现有压缩包 | 是否需要重新打包 |
|------|-----------|-----------------|
| 包含所有必需文件 | ✅ 是 | ❌ 不需要 |
| 包含安装说明 | ✅ 是 | ❌ 不需要 |
| 可以直接交付客户 | ✅ 是 | ❌ 不需要 |

**结论**：直接使用现有的 `禹邻优鲜-小票打印代理.zip` 即可！

---

## ✅ 最终答案

### 给客户这个压缩包：

```
print-agent/禹邻优鲜-小票打印代理.zip
```

### 附带说明：

1. 解压后运行 `setup-agent.cmd` 配置
2. 建议运行 `install-autostart.cmd` 设置开机自启
3. 详细说明查看压缩包内的 `安装说明.txt`

---

**就是这么简单！现有压缩包完全够用，无需重新打包。** 🎉
