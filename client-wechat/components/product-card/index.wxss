.product-card {
  position: relative;
  overflow: hidden;
  background: #ffffff;
  border-radius: 18rpx;
  box-shadow: 0 8rpx 22rpx rgba(8, 34, 20, 0.05);
}

.image-wrap {
  position: relative;
  width: 100%;
  aspect-ratio: 1 / 1;
  padding: 0;
  background: #f4f7f5;
}

.product-image {
  width: 100%;
  height: 100%;
  transition: filter 180ms ease-out, opacity 180ms ease-out;
}

.product-card.is-unavailable .product-image {
  filter: grayscale(72%);
  opacity: 0.56;
}

.sold-out-badge {
  position: absolute;
  left: 50%;
  top: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 128rpx;
  height: 52rpx;
  padding: 0 18rpx;
  color: #ffffff;
  font-size: 23rpx;
  font-weight: 700;
  background: rgba(20, 31, 26, 0.76);
  border-radius: 12rpx;
  transform: translate(-50%, -50%);
}

.badge {
  position: absolute;
  top: 0;
  left: 0;
  padding: 6rpx 14rpx;
  color: #1e5b2b;
  font-size: 20rpx;
  font-weight: 600;
  background: #e9f4df;
  border-radius: 0 0 10rpx 0;
}

.body {
  position: relative;
  display: flex;
  flex-direction: column;
  min-height: 188rpx;
  padding: 18rpx 18rpx 20rpx;
}

.name {
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
  min-height: 74rpx;
  max-height: 72rpx;
  color: #101815;
  font-size: 26rpx;
  font-weight: 700;
  line-height: 36rpx;
}

.subtitle {
  display: block;
  height: 32rpx;
  margin-top: 2rpx;
  overflow: hidden;
  color: #68736c;
  font-size: 22rpx;
  line-height: 32rpx;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
  margin-top: auto;
  padding-top: 12rpx;
}

.price {
  display: flex;
  align-items: baseline;
  flex: 1;
  min-width: 0;
  color: #00833f;
  white-space: nowrap;
}

.product-card.is-unavailable .price {
  color: #707873;
}

.currency {
  font-size: 22rpx;
  font-weight: 700;
}

.value {
  font-size: 32rpx;
  font-weight: 800;
}

.price-from {
  margin-left: 3rpx;
  font-size: 19rpx;
  font-weight: 700;
}

.unit {
  overflow: hidden;
  color: #5b665f;
  font-size: 22rpx;
  text-overflow: ellipsis;
}

.add-button {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  width: 56rpx;
  min-width: 56rpx;
  height: 56rpx;
  min-height: 56rpx;
  padding: 0;
  color: #ffffff;
  font-size: 32rpx;
  font-weight: 800;
  line-height: 1;
  background: #008a45;
  border-radius: 999rpx;
  box-shadow: 0 6rpx 16rpx rgba(0, 122, 61, 0.2);
}

.add-button.is-sku-btn {
  width: 116rpx;
  min-width: 116rpx;
  height: 52rpx;
  min-height: 52rpx;
  background: #008a45;
  border-radius: 999rpx;
}

.sku-button-text {
  color: #ffffff;
  font-size: 22rpx;
  font-weight: 800;
}

.sku-badge {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  min-width: 28rpx;
  height: 28rpx;
  padding: 0 6rpx;
  color: #ffffff;
  font-size: 18rpx;
  font-weight: 800;
  line-height: 28rpx;
  text-align: center;
  background: #ff4b2b;
  border: 2rpx solid #ffffff;
  border-radius: 999rpx;
}

/* Meituan Style Stepper Group: [- Count +] */
.stepper-group {
  display: flex;
  align-items: center;
  gap: 8rpx;
  flex-shrink: 0;
}

.stepper-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 52rpx;
  min-width: 52rpx;
  height: 52rpx;
  min-height: 52rpx;
  padding: 0;
  margin: 0;
  box-sizing: border-box;
  border-radius: 999rpx;
}

.stepper-btn::after {
  border: 0;
}

.minus-btn {
  background: #ffffff;
  border: 2rpx solid #008a45;
}

.add-btn {
  background: #008a45;
  box-shadow: 0 4rpx 12rpx rgba(0, 122, 61, 0.25);
}

.stepper-icon {
  width: 24rpx;
  height: 24rpx;
}

.stepper-count {
  min-width: 32rpx;
  color: #1a1a1a;
  font-size: 26rpx;
  font-weight: 800;
  text-align: center;
}

.add-icon {
  width: 28rpx;
  height: 28rpx;
}

.add-button.is-disabled {
  color: #5f6863;
  background: #dfe4e1;
  box-shadow: none;
}

.sold-out-button-text {
  font-size: 18rpx;
  font-weight: 700;
}

.product-card.glass-mode {
  background: var(--surface);
  border: 1rpx solid var(--surface-line);
  box-shadow: var(--shadow-soft);
  backdrop-filter: blur(var(--glass-blur)) saturate(140%);
  -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(140%);
}

.product-card.glass-mode .image-wrap {
  background: rgba(240, 250, 244, 0.44);
}

.product-card.glass-mode .body {
  background: rgba(255, 255, 255, 0.24);
}
